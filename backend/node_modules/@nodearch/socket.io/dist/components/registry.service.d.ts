import { Logger } from '@nodearch/core';
import * as IO from 'socket.io';
import { INamespaceInfo } from '../interfaces.js';
import { ComponentInfo } from '@nodearch/core/components';
export declare class RegistryService {
    private readonly logger;
    constructor(logger: Logger);
    register(io: IO.Server, namespaceInfo: INamespaceInfo, namespace: ComponentInfo): void;
    private getDefaultMiddleware;
    private onConnection;
    private onAny;
    private onDisconnect;
    private getEventHandler;
    private getEventHandlerArgs;
}
//# sourceMappingURL=registry.service.d.ts.map