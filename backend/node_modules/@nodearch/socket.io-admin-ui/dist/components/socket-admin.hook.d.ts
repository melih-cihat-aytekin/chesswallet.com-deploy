import { AppContext, IHook, Logger } from '@nodearch/core';
import { SocketIOAdminUIConfig } from './socket-admin.config.js';
export declare class SocketAdminHook implements IHook {
    private readonly appContext;
    private readonly config;
    private readonly logger;
    constructor(appContext: AppContext, config: SocketIOAdminUIConfig, logger: Logger);
    onStart(): Promise<void>;
}
//# sourceMappingURL=socket-admin.hook.d.ts.map