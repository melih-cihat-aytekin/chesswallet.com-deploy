import { ConfigManager } from '@nodearch/core';
import { AdminUIOptions, SocketIOAdminUIOptions } from '../interfaces.js';
import { ISocketIOServerProvider } from '@nodearch/socket.io';
export declare class SocketIOAdminUIConfig implements SocketIOAdminUIOptions {
    server: ISocketIOServerProvider;
    options: AdminUIOptions;
    enable: boolean;
    constructor(config: ConfigManager);
}
//# sourceMappingURL=socket-admin.config.d.ts.map